import type { ReactNode } from 'react';

/** Shared shell for the board and its canonical board-framed applicant route. */
export default function TeamLayout({ children }: { children: ReactNode }) {
  return children;
}
